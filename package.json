{
  "name": "servicio-tecnico-backend",
  "version": "2.0.0",
  "description": "API REST — Servicio Técnico y Venta de Repuestos de Televisores (Prisma + PostgreSQL)",
  "main": "server.js",
  "scripts": {
    "build": "npx prisma generate && npx prisma migrate deploy",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prisma:migrate": "npx prisma migrate dev",
    "prisma:studio": "npx prisma studio",
    "seed": "node seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^5.10.0"
  }
}
